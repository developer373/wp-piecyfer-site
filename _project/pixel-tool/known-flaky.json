{
  "_readme": [
    "Artefacts that differ between two captures of an UNCHANGED site.",
    "",
    "Each entry must record how it was proven flaky rather than caused by the",
    "change under test. The proof is always the same control experiment:",
    "capture the same page twice with the change REVERTED. If those two",
    "disagree, the artefact is flaky and belongs here. If they agree, the",
    "difference is real and does not.",
    "",
    "Listed artefacts are still compared and their pixel counts still reported",
    "-- they just do not fail the run. That is deliberate: suppressing them",
    "silently would hide a genuine regression that later lands on the same",
    "artefact. Keep this list as short as the evidence allows, and re-test",
    "entries when the harness changes.",
    "",
    "Markup is never suppressed. A markup change on a flaky artefact still",
    "fails the comparison."
  ],

  "_retired": {
    "our-team@desktop.png": {
      "wasReason": "Elementor motion-fx decorative dot layer rendered its background at a slightly different scale between runs — ~2,900 px, 0.04% of the page.",
      "retiredOn": "2026-08-14",
      "why": "It stopped reproducing once the settle loop dispatched its resize on every pass instead of once before the loop, and once captures cleared Elementor's element cache. Proven over the full ref-a vs ref-b control: 117 screenshots, 0 visual differences, and this artefact silent — under concurrent agent load, which is the condition that used to provoke it. Kept here as a record rather than deleted, so that if it ever returns we know it is a regression and not a new discovery."
    }
  },

  "artefacts": {
  }
}
